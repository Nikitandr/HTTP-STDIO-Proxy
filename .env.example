# HTTP Proxy Server Configuration Example
# Copy this file to .env and modify values as needed

# Server settings
PORT=8000
REQUEST_TIMEOUT=60

# STDIO Server Command Configuration
# Command to run the MCP server (e.g., uv, python, node)
STDIO_COMMAND=uv
# Path to the directory containing your MCP server
STDIO_DIRECTORY=/absolute/path/to/your/mcp/server
# Script name to execute
STDIO_SCRIPT=main.py

# Process management settings
PROCESS_TERMINATION_TIMEOUT=5
PROCESS_START_DELAY=0.5

# HTTP Headers
CONTENT_TYPE=application/json
CORS_ORIGIN=*
CORS_HEADERS=Content-Type
